---
import { getWeather } from "@api/getWeather";
import Layout from "../../layouts/Layout.astro";

const result = await getWeather();
const theme = result?.theme;
---

<Layout title="contact | 株式会社loadoff" theme={theme}>
  <main>
    contact
  </main>
</Layout>
