---
import { getWeather } from "@api/getWeather";
import Layout from "../../layouts/Layout.astro";

const result = await getWeather();
const theme = result?.theme;
---

<Layout title="recruit | 株式会社loadoff" theme={theme}>
  <main>
    recruit
  </main>
</Layout>
