---
import { getWeather } from "@api/getWeather";
import Layout from "../../layouts/Layout.astro";

const result = await getWeather();
const theme = result?.theme;
---

<Layout title="Service | 株式会社loadoff" theme={theme}>
  <main>
    サービス
  </main>
</Layout>
